{
  "name": "socio-design-web",
  "version": "0.1.0",
  "author": "Johannes Oster <johannes.oster@outlook.at>",
  "license": "MIT",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "rm -rf ./dist && parcel build src/index.html --dist-dir ./dist --public-url ./ --no-cache",
    "deploy": "yarn build && gh-pages -d dist"
  },
  "targets": {
    "main": {
      "includeNodeModules": true
    }
  },
  "dependencies": {
    "cytoscape": "^3.27.0",
    "cytoscape-cola": "^2.5.1"
  },
  "devDependencies": {
    "@types/cytoscape": "^3.19.16",
    "gh-pages": "^6.1.0",
    "parcel": "^2.10.2"
  }
}
